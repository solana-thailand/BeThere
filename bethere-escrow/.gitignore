# Build artifacts
/target

# Lock files
# Cargo.lock is TRACKED — bethere-escrow is a deployed on-chain program, so its
# dependency graph (esp. the pre-release quasar deps) must be pinned for a
# reproducible/verifiable build.
package-lock.json
yarn.lock

# Dependencies
node_modules

# Environment
.env
.env.*

# OS
.DS_Store
