# Rust
/target/
debug/
*.pdb

# Environment
.env
.env.*
!.env.example
!.env.staging.example
.dev.vars

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
.logs/

# D1 backups (contain PII — never commit)
backup-*.sql
*.sql.bak

# Build artifacts
*.d
*.rlib
*.wasm

# Merge conflict leftovers
*.orig
*.rej

# Node (if any tooling uses it)
node_modules/

# Leptos / WASM frontend
frontend-leptos/target/
frontend-leptos/dist/
frontend-leptos/.cargo/
# frontend-leptos/Cargo.lock is tracked (deployed binary → pin deps, incl.
# wasm-bindgen, so builds are reproducible and match the wasm-bindgen CLI).

# Cloudflare Worker
worker/build/
worker/.wrangler/
worker/.dev.vars

# Wrangler local state (root level)
.wrangler/

# Brand assets (logos embedded inline in icons/mod.rs)
Nov 2024 2/
solflare-brand-kit/
Backpack Media Kit/
jupiter-brand-kit/
.DS_Store

# cNFT scripts (standalone Rust project)
scripts/cnft/target/
scripts/cnft/Cargo.lock
__pycache__/

# Generated metrics snapshot (regenerated by scripts/measure_metrics.py)
scripts/.metrics.json

# Academic deliverables (internal, not for public repo)
.deliverables/
.issues/031_*

.dev.vars*
!.dev.vars.example
target

# Claude Code personal overrides (project settings.json stays tracked)
.claude/settings.local.json
