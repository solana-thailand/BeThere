# BeThere — Evidence Ledger

> Single source of truth for every numeric claim in the pitch deck (`scripts/make_pitch_deck.py`), `README.md`, and `DEMO.md`.
> Every entry below is either (a) backed by a fetched primary/secondary source with a quote and date, or (b) generated from the live codebase by `scripts/measure_metrics.py`, or (c) marked `UNVERIFIED`.
> **No claim in this ledger is fabricated.** Where confidence is low, it says so plainly.

**Last regenerated:** 2026-06 (run `python3 scripts/measure_metrics.py` to refresh the self-measured rows; external rows are dated per-source).

## How to read this

- **Category** — `external` (independent source), `self` (measured from our code), `comparison` (derived from two values), `feature` (competitive capability).
- **Confidence** — `high` (measured/primary-sourced), `medium` (secondary source or assumption-dependent), `low` (UNVERIFIED / needs work).
- A `matches_deck` note means the value was cross-checked against the figure shown on the slides.

---

## 1. External statistics

| Claim | Value | Source | Checked | Confidence | Method |
|---|---|---|---|---|---|
| Free-event no-show rate | **30–40%** (deck, conservatively within the documented range) | Eventbrite 2024 Event Trends Report — *"free events experience no-show rates between 20% and 50%"* — cited via realevents.co | 2026-06 | medium (secondary; primary report not fetched directly — see caveat) | Corroborated by whos-in.app (40–60%), conferencesthatwork.com ("well below 50%"), grabmyslot.com (10–20%) |
| No-show rate **with** a deposit | **3–7%** (validates our deposit thesis) | grabmyslot.com (2026) — *"with a deposit requirement… fall to 3 to 7 percent"* | 2026-06 | medium | Direct quote; secondary blog source |

> **Caveat (no-show):** the Eventbrite 2024 figure is cited **via** realevents.co (a secondary source). To upgrade to `high`, fetch Eventbrite's report directly. The deck's `30–40%` is **conservative** (middle of the 20–60% documented range), so the claim is defensible as-is.

## 2. Self-measured metrics (codebase)

> All rows below are regenerated by `scripts/measure_metrics.py` (stdlib-only, exit 0). Run it to refresh.

| Claim | Value | Source / derivation | Confidence | matches deck |
|---|---|---|---|---|
| On-chain program size | **89,856 bytes (88 KB)** | `os.stat().st_size` on `bethere-escrow/target/deploy/bethere_escrow.so` (built artifact, mtime 2026-06-03) | high | ✅ |
| Kani proof harnesses | **16** | count of `#[kani::proof]` in `bethere-escrow/src/kani.rs` | high | ✅ |
| Per-transaction fee | **$0.000869** (deck shows $0.00087) | **Measured** 5,051 lamports (real deposit TX, `.handovers/052` E2E devnet run, sig `4cQnNGRa…`) ÷ 1e9 × $172/SOL = $0.0008688. Theoretical base-fee floor (5,000 lamports) = $0.00086; the measured TX is what the deck reports. | high | ✅ (measured value rounds to $0.00087) |
| Executed Rust tests | **250 passing** (54 on-chain + 73 domain + 123 worker) | `cargo test` summary lines per crate | high | ✅ (deck says "250+") |
| Frontend Leptos test specs | **147** (static count) | regex over `#[test]`/`#[wasm_bindgen_test]` in `frontend-leptos` | medium | docs only (not on slides) |

## 3. Comparison claims (derived)

| Claim | Value | Methodology | Confidence | matches deck |
|---|---|---|---|---|
| cNFT vs POAP cost | **~50× cheaper** (floor) | BeThere cNFT **$0.001** vs POAP-on-Gnosis **$0.05** (organizer floor) = 50×. Full range 50–200× at $0.05–0.20. **NOT** vs Ethereum mainnet (POAP avoids mainnet by design). | medium — see caveat | ✅ |
| 1,000-badge cost | **$1.00 vs $50–200** (POAP on Gnosis) | **Like-for-like (badges vs badges):** 1,000 × $0.001 (BeThere cNFT) = $1.00 vs 1,000 × $0.05–0.20 (POAP mints on Gnosis) = $50–200. Floor multiplier = exactly 50× ($1.00 ÷ $50). Previous deck figure ($0.87) conflated check-in TX fees with badge mints — corrected to a pure badge-to-badge comparison. | medium — see caveat | ✅ |

## 4. Competitive-feature claims

> Snapshot **2026-06-15**. All cells verified against official help docs (fetched, quoted below). The deck's claim — *"Luma & Eventbrite have no deposits, no penalties, no real skin in the game"* — **holds up** at the mechanism level.

| Platform × Feature | Supported? | Evidence (official, fetched 2026-06-15) | Confidence |
|---|---|---|---|
| Luma — RSVP deposit | No | Tickets are "Free or Paid" only; no stake/deposit feature (help.luma.com/p/setting-up-ticket-types) | high |
| Luma — auto-refund on attendance | No | *"Luma does not automatically issue refunds"*; refunds are manual (help.luma.com/p/refunding-a-guest) | high |
| Luma — no-show penalty | No | No penalty/forfeiture concept anywhere in help center | high |
| Luma — attendance NFT/POAP | No | Crypto features are token-*gating* + wallet collection only — not issuance (help.luma.com/p/crypto-features-in-luma) | high |
| Eventbrite — RSVP deposit | No | Tickets are "Paid, Free, or Donation"; "deposit" search returns only payout/banking results | high |
| Eventbrite — auto-refund on attendance | No | Its "automatic refund" only auto-approves pre-event *requests*; check-in triggers no refund | high |
| Eventbrite — no-show penalty | No | No "no-show" status; searches are substring false positives | high |
| Eventbrite — attendance NFT/POAP | No | Help-center searches for "NFT"/"POAP"/"blockchain" return **zero** results | high |

> **Precision note:** both platforms DO support ordinary *paid tickets*, so an attendee who buys a ticket has *some* sunk-cost commitment. The airtight, mechanism-level framing — *"no refundable deposit, no auto-refund-on-check-in, no no-show penalty, no on-chain credential"* — is 100% supported.

## 5. Impact framing (SDG 12.3)

| Claim | Value | Causal chain | Confidence |
|---|---|---|---|
| Aligned with UN SDG 12.3 | "Halve food waste by 2030" | fewer no-shows → less over-catering → less food waste → SDG 12.3. Rests on the §1 no-show stat (medium confidence). | medium |

---

## Caveats / lower-confidence claims (read before pitching)

These are the items an informed investor is most likely to probe. Each is documented so you can answer truthfully.

1. **cNFT cost — $0.001 vs $0.00206 (the headline number's biggest uncertainty).**
   The deck uses **$0.001** as the marginal mint cost (base TX fee + compute, tree rent amortized over a large Merkle tree — defensible). `measure_metrics.py` derives **$0.00206** from a 12,000-lamports Bubblegum-leaf model (more conservative). At $0.002 the "~50× cheaper" floor becomes ~25×. **If you want maximum defensibility, swap $0.001 → $0.002** (a one-line deck edit); the story still holds. Flipping is localized and reversible.

2. **Check-in latency — "< 500ms" is UNVERIFIED.**
   Edge-deployed; cannot be measured statically. Requires a live benchmark against the deployed Cloudflare worker (`curl -w '%{time_total}'` on `/api/checkin`). Until then it is a design target, not a measured figure. Flagged `NEEDS_BENCHMARK` by the metrics script.

3. **Test count — "250+" vs 397 total.**
   The deck says **250+** = the *executed and verified-passing* Rust tests (54 on-chain + 73 domain + 123 worker). A further **147** frontend (Leptos/WASM) test specs exist as a static count but were **not executed** by the metrics script (wasm runtime not invoked), so they are documented separately rather than claimed "passing." If CI runs `wasm-pack test --headless` successfully, the headline can move to 397.

4. **No-show source is secondary.**
   Eventbrite 2024 figure is cited via realevents.co, not fetched from Eventbrite directly. The deck's 30–40% is conservative either way.

---

## Regenerate

```
python3 scripts/measure_metrics.py   # refreshes §2 self-measured rows + scripts/.metrics.json
```

External rows (§1, §4) are point-in-time snapshots; re-verify them before a funding round.
````
